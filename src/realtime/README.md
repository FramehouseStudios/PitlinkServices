# realtime module

Provider presence (TTL heartbeats; Redis live impl + in-memory mirror),
PresenceProviderDirectory for live supply, in-process RequestEventBus, and
the member-scoped WebSocket status surface. See docs/HANDOFF.md increment 6
notes.
