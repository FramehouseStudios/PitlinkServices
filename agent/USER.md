# USER.md — Human Context

> Who the agent works for and how they prefer to work. Update only when the
> founder's stated preferences, context, or priorities materially change. Do
> not invent personal details.

- **As of:** 2026-08-26

## Who the agent is working with

Founder / operator of Pitlink (Framehouse Studios; GitHub org:
`FramehouseStudios`; email `framehousestudios2001@gmail.com`). Building an
AI-native mobility assistance platform — "make being stranded a solved
problem." Currently Phase 0 (Foundation): proving the core request loop with
an evidence spine before any live marketplace work.

## Communication preferences

- Direct, high-signal responses. Lead with the outcome.
- Every material task ends with the Completion Standard report
  (`brain.md` §8) — the founder evaluates work against it.
- Strict evidence labels; refusing to invent commercial premises is valued,
  not treated as unhelpfulness.
- Expects the agent to stop cleanly on the six founder-owned RFIs
  (`brain.md` §7) rather than guess — but otherwise to keep moving: assume,
  label, continue.
- Expects an adversarial self-pass on material work before it is declared done,
  with the attempt and result documented.
- "CONTINUE" from the founder means: proceed to the next Delivery Plan
  increment and push completed, verified work to GitHub.

## Working context

- Timezone: Pacific. Platform: macOS, shell zsh, via Claude Code.
- Repository: `FramehouseStudios/PitlinkServices`, local at
  `~/Pitlink Services` (note the space in the path — quote it in shell).
- Standing instruction (2026-08-26): keep the GitHub repo and the local folder
  in sync — commit and push verified work without waiting to be asked.
- The founder decided the runtime: Node.js + TypeScript [DECIDED 2026-08-26].

## Active priorities

1. Complete the Phase 0 Delivery Plan increments in order (see
   `docs/HANDOFF.md` for the progress table).
2. Reach the Phase 0 exit: one end-to-end request — created, matched (mock),
   tracked, closed — with a reproducible evidence timeline.
3. Keep every increment inside the invariants: evidence spine, identity
   isolation, Contract Gate, idempotency, no hard-coded commercial terms.
